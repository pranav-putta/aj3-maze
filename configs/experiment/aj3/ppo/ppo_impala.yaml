# @package _global_

defaults:
  - aj3/ppo/base_ppo
  - override /net@trainer.agent.policy: base_impala_net
  - _self_

trainer:
  agent:
    policy:
      in_dim: ${trainer.env.size[0]}
      embd_vocab_size: 4
      embd_dim: 64
      hidden_dim: 64
      out_dim: 4
      rnn_layers: 2

